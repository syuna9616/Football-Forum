<div class="d-flex align-items-center">
  <div class="ml-auto boards__linkBox">
    <%=link_to "掲示板一覧", boards_path, class: "btn btn-outline-primary" %>
  </div>
</div>
<%= render partial: "boards/flash" %>
<%= render partial: "boards/board_form", locals: {board: @board} %>

<%= render partial: "comments/comment_form", locals: {comment: @comment }%>

<div class="p-comment__list">
  <div class="p-comment__listTitle">コメント</div>
  <%= render @board.comments %>
</div>
